"""
setup.py — minimal shim for editable installs.

blg-model-builder is a pure-Python package.
All interatomic potentials use the LAMMPS Python module.

Install with:
    pip install -e .
"""
from setuptools import setup

setup()
